# Local orchestrator
PORT=8000

# Adapters
ARM_ADAPTER=mock   # mock|http
VISION_ADAPTER=mock # mock|webcam_min2
TTS_ADAPTER=local_player

# Personality
DEFAULT_STYLE=polite # polite|meme
DEFAULT_SAFE=true

# If ARM_ADAPTER=http
ARM_HTTP_BASE_URL=http://127.0.0.1:9000

# Camera device index (mac)
CAMERA_INDEX=0
