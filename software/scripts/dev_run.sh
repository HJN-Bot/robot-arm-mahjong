#!/usr/bin/env bash
set -euo pipefail

# placeholder; on Mac:
# python -m venv .venv && source .venv/bin/activate
# pip install -r requirements.txt
# uvicorn software.web.app:app --reload --port 8000

echo "TODO: run uvicorn software.web.app:app --reload --port 8000"
